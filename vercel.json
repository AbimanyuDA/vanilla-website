{
  "buildCommand": "npm install && npm run build && composer install",
  "installCommand": "npm install && composer install",
  "functions": {
    "public/index.php": {
      "runtime": "php-8.3"
    }
  },
  "routes": [
    {
      "src": "/build/(.*)",
      "dest": "/public/build/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/public/index.php"
    }
  ]
}
